# # After setting up Anthropic client and SYSTEM_PROMPT
# git add core/llm.py
# git commit -m "feat(core): llm.py — Anthropic client setup and safety system prompt"

# # After writing run_inference with full prompt construction
# git add core/llm.py
# git commit -m "feat(core): llm.py — run_inference builds prompt from payload and RAG context"

# # After adding JSON parsing and fallback dict
# git add core/llm.py
# git commit -m "feat(core): llm.py — add JSON parsing with safe fallback on parse error"

# # After testing with real symptoms and RAG context
# git add core/llm.py
# git commit -m "test(core): llm.py — verified LLM returns valid JSON diagnosis for test symptoms"